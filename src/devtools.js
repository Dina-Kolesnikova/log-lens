/* Log Lens — devtools bootstrap. Registers the "⌕ Log Lens" panel next to
 * Network. All the actual work happens in devtools-panel.js. */
chrome.devtools.panels.create('⌕ Log Lens', '/icons/32.png', '/src/devtools-panel.html');
